# Backend (placeholder)

Sẽ dựng bằng NestJS + PostgreSQL (Prisma/TypeORM) ở giai đoạn sau, khi làm phần catalog sản phẩm trà, đơn hàng, khách hàng, vườn trà đối tác...

Khởi tạo gợi ý:

```bash
cd apps/backend
npx @nestjs/cli new . --package-manager pnpm --skip-git
```
